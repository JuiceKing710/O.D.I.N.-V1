{
  "permissions": [
    {
      "name": "read_files",
      "description": "Read user-selected local files.",
      "default": "prompt"
    },
    {
      "name": "write_files",
      "description": "Create or modify local files.",
      "default": "prompt"
    },
    {
      "name": "execute_scripts",
      "description": "Execute local scripts or shell commands.",
      "default": "denied"
    },
    {
      "name": "access_network",
      "description": "Access external network resources.",
      "default": "prompt"
    },
    {
      "name": "generate_images",
      "description": "Generate images from text prompts.",
      "default": "prompt"
    },
    {
      "name": "control_desktop",
      "description": "Control native macOS apps (activate, type, click menus).",
      "default": "prompt"
    },
    {
      "name": "observe_screen",
      "description": "Capture the screen for on-device visual analysis.",
      "default": "prompt"
    }
  ]
}

